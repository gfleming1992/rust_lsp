# 3.3.4 The scale attribute

The scale attribute is of data type scaleType. The scale attribute is a “double” that must have a value greater than zero. All x and y dimensions of a geometry are multiplied by the scale attribute. The scale factor does not apply to angular values. The default value is 1.0.
